/* eslint-env browser */

var BubbleChart = BubbleChart || {};

BubbleChart.BubbleChartModel = function () {
  "use strict";

  var that = {};

  function getAllData() {
    //
  }

  function getTimelineData() {
    // wahl der Saison, dann visualisierung
  }

  function getSeasonData() {
    // wahl der Saison, dann visualisierung
  }

  that.getAllData = getAllData;
  that.getTimelineData = getTimelineData;
  that.getSeasonData = getSeasonData;
  return that;
};
